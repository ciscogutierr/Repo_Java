import javax.naming.Name;

public class Homework9 {
}
public class Homework9 {
    public static void main(String[] args) {
        // Due: Mar 25, 2021
        /**
         * Write a method to convert given string into Titlecase
         */

        System.out.println(toTittlecase);
        public static String toTittlecase(String myString) {
            if (myString.trim().length() > 0) {
                String[] myStringArray = myString.trim.split("\\s+");
                myString = " ";
                for (int i = 0; i <= myStringArray.length; i++) {
                    myString = myString + myStringArray[i].substring(0, 1).toUpperCase() + myStringArray[i].substring(0, 1).toLowerCase();
                }
            } else {
                myString = "Please enter a valid string";
            }
            return myString;
        }

        /**
         * Write a method that will tell grade to student for given score and MaxScore
         */
        /**
         * Write a method that will reverse any given int-array
         */
        public static int[] reverseNums(int[],nums){
            int[] revNum= new int[nums.length]
                    for(int i= 0; i<= reverseNums.length; i++){
                        int rev= nums[i];
                        nums[i]= nums.length
                    }
        }



        /**
         * Write a method that return the number of words in given sentence.
         */
    }
}